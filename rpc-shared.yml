# To be used with ronin.yml. Bind to localhost by default; place the node
# behind ext-network.yml/Traefik for public HTTPS exposure.
services:
  ronin-execution:
    ports:
      - 127.0.0.1:${RPC_PORT:-8545}:8545/tcp
      - 127.0.0.1:${WS_PORT:-8546}:8546/tcp
